---
const { bgColor = "", innerClass = "" } = Astro.props;
---

<section class={`w-full py-10 ${bgColor}`}>
    <div
        class={`max-w-[1440px] mx-auto px-10 lg:px-20 flex flex-col gap-10 ${innerClass}`}
    >
        <slot />
    </div>
</section>
